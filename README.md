# Wallet Transfer Service

A Spring Boot 3 + Java 21 wallet transfer service backed by PostgreSQL.

The service demonstrates how to implement a reliable wallet-to-wallet money transfer with:

* Idempotent APIs
* Double-entry ledger
* Atomic database transactions
* Pessimistic locking
* Deterministic lock ordering for deadlock avoidance
* Transaction state management
* PostgreSQL constraints and indexes
* Behavioral integration testing with Testcontainers

---

## 1. What This Service Does

The service allows money to be transferred between wallets.

Example:

```text
wallet_1
Balance: 1000.00

        │
        │ Transfer 100.00
        ▼

wallet_2
Balance: 500.00
```

After the transfer:

```text
wallet_1 = 900.00
wallet_2 = 600.00
```

A successful transfer creates exactly two ledger entries:

```text
DEBIT   wallet_1   100.00
CREDIT  wallet_2   100.00
```

The balance update and ledger entries are committed as a single database transaction.

---

## 2. Technology Stack

| Component           | Technology                  |
| ------------------- | --------------------------- |
| Language            | Java 21                     |
| Framework           | Spring Boot 3               |
| Database            | PostgreSQL                  |
| ORM                 | Spring Data JPA / Hibernate |
| Build               | Maven                       |
| Integration Testing | JUnit + Testcontainers      |
| Container Runtime   | Docker                      |

---

## 3. Architecture

The application follows a layered architecture:

```text
HTTP Request
     │
     ▼
Controller / API
     │
     ▼
TransferService
     │
     ├── Idempotency validation
     ├── Request validation
     └── Existing transfer lookup
     │
     ▼
TransferTransactionService
     │
     ├── @Transactional
     ├── Lock source wallet
     ├── Lock destination wallet
     ├── Validate balance
     ├── Update balances
     ├── Create ledger entries
     └── Update transfer state
     │
     ▼
PostgreSQL
```

`TransferService` and `TransferTransactionService` are separate Spring beans.

This is intentional because Spring's proxy-based `@Transactional` handling does not apply when a transactional method is called through self-invocation within the same bean.

---

## 4. Database Design

The service uses three main tables.

### Wallets

Stores the current balance of each wallet.

```text
wallets
--------------------------------
wallet_id       PK
balance
status
version
created_at
updated_at
```

The wallet uses an optimistic version field for entity consistency, while transfer execution uses pessimistic database locking.

### Transfers

Stores the transfer request and its current state.

```text
transfers
--------------------------------
transfer_id      PK
idempotency_key  UNIQUE
from_wallet_id
to_wallet_id
amount
status
created_at
processed_at
```

Transfer states:

```text
PENDING
   │
   ├──► PROCESSED
   │
   └──► FAILED
```

### Ledger Entries

Stores the accounting entries generated by successful transfers.

```text
ledger_entries
--------------------------------
id              PK
wallet_id
transfer_id
entry_type
amount
created_at
```

Each successful transfer produces exactly two entries:

```text
DEBIT  → source wallet
CREDIT → destination wallet
```

The ledger is append-only. Ledger entries should not be modified or deleted after creation.

---

## 5. Idempotency

Every transfer request requires an `idempotencyKey`.

Example:

```json
{
  "idempotencyKey": "abc123",
  "fromWalletId": "wallet_1",
  "toWalletId": "wallet_2",
  "amount": 100
}
```

The database enforces uniqueness:

```text
idempotency_key UNIQUE
```

If the same request is submitted again:

```text
Request 1
    │
    └── abc123 → Transfer executed

Request 2
    │
    └── abc123 → Existing transfer returned
```

The second request does not execute the transfer again.

If the same key is reused with different transfer parameters, the request is rejected.

For example:

```text
abc123
wallet_1 → wallet_2
100.00
```

followed by:

```text
abc123
wallet_1 → wallet_2
200.00
```

is treated as an idempotency conflict.

---

## 6. Transaction Strategy

A transfer is executed inside a single database transaction.

The following operations are part of the same transaction:

```text
1. Lock wallets
2. Validate wallet state
3. Validate available balance
4. Debit source wallet
5. Credit destination wallet
6. Create DEBIT ledger entry
7. Create CREDIT ledger entry
8. Mark transfer PROCESSED
```

If any operation fails, the transaction is rolled back.

This prevents situations such as:

```text
Wallet debited
      ↓
Application failure
      ↓
Ledger entry missing
```

Instead, the database transaction guarantees that the related changes are committed or rolled back together.

---

## 7. Concurrency Handling

Wallet rows are locked using pessimistic locking:

```text
SELECT ... FOR UPDATE
```

This prevents concurrent transfers from modifying the same wallet simultaneously.

For example, if multiple requests attempt:

```text
wallet_1 → wallet_2
wallet_1 → wallet_3
wallet_1 → wallet_4
```

the database serializes access to `wallet_1`.

### Deadlock avoidance

When both source and destination wallets need to be locked, the service locks them in deterministic wallet ID order.

For example:

```text
Transfer A:
wallet_1 → wallet_2

Transfer B:
wallet_2 → wallet_1
```

Both transactions acquire locks in the same deterministic order:

```text
wallet_1
   ↓
wallet_2
```

This avoids the classic:

```text
Transaction A:
lock wallet_1
waiting for wallet_2

Transaction B:
lock wallet_2
waiting for wallet_1
```

deadlock pattern.

---

## 8. Failure Handling

### Insufficient balance

If the source wallet does not have enough funds:

```text
Transfer
   │
   ▼
Check balance
   │
   ├── insufficient
   ▼
FAILED
```

No wallet balance or ledger entry is modified.

### Invalid self-transfer

A transfer where:

```text
fromWalletId == toWalletId
```

is rejected.

### Missing wallet

A transfer referencing a non-existent wallet is rejected.

### Idempotency conflict

Reusing an existing idempotency key with different transfer parameters is rejected.

---

## 9. API

### Create Transfer

```http
POST /wallet/v1/transfers
```

Request:

```json
{
  "idempotencyKey": "abc123",
  "fromWalletId": "wallet_1",
  "toWalletId": "wallet_2",
  "amount": 100
}
```

Example:

```bash
curl -X POST http://localhost:8080/wallet/v1/transfers \
  -H "Content-Type: application/json" \
  -d '{
    "idempotencyKey": "abc123",
    "fromWalletId": "wallet_1",
    "toWalletId": "wallet_2",
    "amount": 100
  }'
```

Expected result:

```text
wallet_1 = 900.00
wallet_2 = 600.00
```

and exactly two ledger entries are created.

---

## 10. Initial Wallet Data

The development environment contains:

| Wallet   | Balance |
| -------- | ------: |
| wallet_1 | 1000.00 |
| wallet_2 |  500.00 |

These values are intended for local development and testing.

---

## 11. Prerequisites

Install:

* Java 21
* Maven
* Docker
* Docker Compose

Verify:

```bash
java -version
mvn -version
docker --version
docker compose version
```

---

## 12. Start PostgreSQL

Start the local PostgreSQL instance:

```bash
docker compose up -d
```

Verify that PostgreSQL is ready:

```bash
docker exec -it wallet-postgres pg_isready -U wallet -d walletdb
```

Connect to PostgreSQL:

```bash
docker exec -it wallet-postgres psql -U wallet -d walletdb
```

---

## 13. Database Schema Management

Hibernate should validate the database schema rather than modify it automatically:

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate
```

Database schema changes should be managed through versioned migrations.

This keeps the database schema reproducible when a new developer clones the repository.

The startup flow should be:

```text
Application startup
       │
       ▼
Database migrations
       │
       ▼
PostgreSQL schema
       │
       ▼
Hibernate validation
       │
       ▼
Application starts
```

---

## 14. Run the Application

Start the application with:

```bash
mvn spring-boot:run
```

The service will start on:

```text
http://localhost:8080
```

---

## 15. Run Tests

The integration tests use Testcontainers.

Testcontainers starts an isolated PostgreSQL container, allowing the tests to execute against a real PostgreSQL database rather than an in-memory or mocked database.

Run the complete test suite:

```bash
mvn clean test
```

The tests cover:

1. Successful transfer
2. Correct wallet balances
3. Exactly two ledger entries
4. Sequential idempotent retry
5. Idempotency-key conflicts
6. Insufficient funds
7. Self-transfer validation
8. Missing wallets
9. Amount validation
10. Concurrent transfers from the same wallet
11. Prevention of double spending
12. Concurrent same-key requests
13. Concurrent transfers to different wallets
14. Opposite-direction transfers
15. Mixed duplicate and unique requests
16. Transaction/concurrency behavior

The tests assert observable behavior such as:

* Wallet balances
* Transfer status
* Number of transfers
* Ledger entries
* Concurrent execution results

rather than testing private methods or implementation details.

---

## 16. Project Structure

```text
src/main/java/com/example/wallettransfer
├── api
├── config
├── domain
├── repository
└── service
    ├── WalletService.java
    └── TransferTransactionService.java

src/test/java/com/example/wallet
└── TransferBehaviorIntegrationTest.java
```

### Main responsibilities

#### `api`

Contains REST controllers and request/response models.

#### `domain`

Contains the wallet, transfer and ledger domain entities.

#### `repository`

Contains Spring Data JPA repositories and database access.

#### `service`

Contains business logic and transaction orchestration.

`WalletService` handles:

* Request validation
* Idempotency checks
* Existing transfer lookup

`TransferTransactionService` handles:

* Database transaction boundary
* Wallet locking
* Balance updates
* Ledger creation
* Transfer state transitions

---

## 17. Design Decisions

### Why PostgreSQL?

The transfer operation requires transactional guarantees, row-level locking, unique constraints and precise decimal monetary values. PostgreSQL provides these database capabilities directly.

### Why pessimistic locking?

A wallet balance is shared mutable state. Pessimistic row locking ensures that concurrent transfers modifying the same wallet are serialized at the database level.

### Why idempotency keys?

Network failures can cause clients to retry a request even when the original request was already processed.

The idempotency key allows the service to distinguish:

```text
Retry of the same operation
```

from:

```text
A new operation
```

### Why a double-entry ledger?

A transfer affects two wallets. Recording both sides explicitly provides an auditable representation of the movement:

```text
DEBIT  source wallet
CREDIT destination wallet
```

### Why separate transaction service?

Spring's `@Transactional` is proxy-based. Keeping the transactional operation in a separate Spring bean ensures that the transaction boundary is applied when the method is invoked.

---

## 18. Development Workflow

After cloning the repository:

```bash
git clone <repository-url>
cd <repository-directory>
```

Start PostgreSQL:

```bash
docker compose up -d
```

Run the application:

```bash
mvn spring-boot:run
```

Run the complete test suite:

```bash
mvn clean test
```

Before committing changes:

```bash
mvn clean test
```

The expected result is that all integration tests pass.

---

## 19. Key Properties of the Implementation

The implementation is designed around the following invariants:

```text
Successful transfer
    ↓
Source balance decreases
    ↓
Destination balance increases
    ↓
One DEBIT ledger entry
    +
One CREDIT ledger entry
    ↓
All committed atomically
```

And:

```text
Same idempotency key
        ↓
Same transfer parameters
        ↓
Return existing transfer
        ↓
No second balance mutation
```

While:

```text
Same idempotency key
        +
Different parameters
        ↓
Reject request
```

These invariants are enforced through a combination of application logic, database constraints, transactions and integration tests.
